services:
  backend:
    build: ./Backend
    ports:
      - "5000:3000"
    volumes:
      - ./Backend:/app
      - /app/node_modules
    command: npx nodemon -L /app/server.js
    environment:
      - NODE_ENV=development